using Project_Backend_2024.Facade.Interfaces;
using Project_Backend_2024.Facade.Models;

namespace Project_Backend_2024.Services.TokenValidators;

public static class UserAccessValidator
{
    public static void ValidateAndGenerate(RefreshRequest refreshRequest)
    {
       //var modelByToken = await _refreshTokenRepository.GetByToken(refreshRequest.refreshToken);
    }
}